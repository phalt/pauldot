{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"pauldot","text":"<pre><code>                   _     _       _\n _ __   __ _ _   _| | __| | ___ | |_\n| '_ \\ / _` | | | | |/ _` |/ _ \\| __|\n| |_) | (_| | |_| | | (_| | (_) | |_\n| .__/ \\__,_|\\__,_|_|\\__,_|\\___/ \\__|\n|_|\n</code></pre> <p>Pauldot is a terminal and tooling configuration manager for users who want to keep their configuration across multiple machines in sync.</p> <p>Use it to manage:</p> <ul> <li>Aliases</li> <li>Dotfiles</li> <li>Tools</li> <li>Environment variables</li> <li>Profiles</li> </ul>"},{"location":"#install","title":"Install","text":"<pre><code>uv tool install pauldot\n</code></pre>"},{"location":"#key-concepts","title":"Key concepts","text":"<p>Your dotfiles are all in one place and synced across your machines.</p> <p>Pauldot is a CLI tool that reads a dotfiles repo and generates <code>~/.zshrc</code> output and installs / updates tools.</p> <p>Your dotfiles are stored in a git repo and synced to remote origin when you run <code>pauldot sync</code>.</p> <p>The CLI tool is configurable via <code>~/.config/pauldot/state.toml</code> so if you change repo URL or branch, <code>pauldot sync</code> will use the new configuration.</p> <p>The generated <code>~/.zshrc</code> is just like any other shell config file.</p> <p>There is no symlink or other magic involved: the generated <code>~/.zshrc</code> is just a regular shell script that gets sourced by your shell. It is compiled by Pauldot from the contents in your dotfiles repo.</p>"},{"location":"#flows","title":"Flows","text":"I want to\u2026 Go to Set up pauldot for the first time Bootstrap a new machine Add pauldot to a machine that already has dotfiles Migrate an existing machine Add an alias and get it onto all my machines Alias lifecycle Declare a new tool and install it everywhere Tool lifecycle Create a work profile or switch profiles Profile lifecycle Track a dotfile like <code>.gitconfig</code> across machines Track a dotfile"},{"location":"#commands","title":"Commands","text":"<pre><code>pauldot init [&lt;repo-url&gt;]        Clone your dotfiles repo and configure this machine\npauldot init --scaffold &lt;path&gt;   Generate a starter dotfiles repo structure\npauldot apply                    Reconcile current profile (zshrc + dotfiles + tools)\npauldot apply --overwrite        Also overwrite existing dotfiles from repo (backs up first)\npauldot status                   Dry-run apply \u2014 show what would change\npauldot doctor                   Health check\n\npauldot migrate                  Migrate an existing ~/.zshrc into your dotfiles repo\npauldot migrate --dry-run        Preview what would be migrated without writing anything\n\npauldot track &lt;path&gt;             Start tracking a dotfile: copy it into the repo\n\npauldot profile show             Show the active profile\npauldot profile list             List available profiles\npauldot profile set &lt;name&gt;       Switch profile\npauldot profile set &lt;name&gt; --apply  Switch profile and apply immediately\n\npauldot tool list                List all defined tools and install status\npauldot tool install [&lt;name&gt;]    Install a tool (or all profile tools)\npauldot tool add                 Interactively add a tool to tools.toml\npauldot tool remove &lt;name&gt;       Remove a tool from tools.toml\n\npauldot alias add &lt;key&gt; &lt;value&gt;  Add an alias to aliases.zsh\npauldot alias list               List defined aliases\n\npauldot sync                     Pull latest changes; push local commits\npauldot clean                    List backup files left by pauldot (dry-run)\npauldot clean --yes              Delete all backup files\npauldot absorb                   Absorb external zshrc modifications into source files\npauldot absorb --dry-run         Show what would be absorbed without writing\npauldot absorb --target &lt;file&gt;   Absorb into a specific source file (default: zshrc.base)\npauldot edit [profile|tools|zshrc|pauldot]  Open a dotfiles file in $EDITOR\n\npauldot help bootstrap           Bootstrap walkthrough for new machines\npauldot help gh                  GitHub CLI auth walkthrough\npauldot help fork                How to set up your own dotfiles repo\n</code></pre>"},{"location":"flows/aliases/","title":"Alias lifecycle","text":"<p>This flow covers adding a new alias on one machine, applying it, syncing it to the remote, and picking it up on a second machine.</p>"},{"location":"flows/aliases/#overview","title":"Overview","text":"<pre><code>sequenceDiagram\n    actor U as You (Machine A)\n    participant P as pauldot\n    participant R as Dotfiles repo (GitHub)\n    actor M as Machine B\n\n    U-&gt;&gt;P: pauldot alias add ll \"ls -la\"\n    P-&gt;&gt;R: Appends alias to files/aliases.zsh\n    P-&gt;&gt;R: git commit (if auto_commit = true)\n    P-&gt;&gt;U: Rewrites ~/.zshrc to include new alias\n\n    Note over U: Open a new shell \u2014 ll works \u2713\n\n    U-&gt;&gt;P: pauldot sync\n    P-&gt;&gt;R: git push\n\n    Note over M: Later, on Machine B\n\n    M-&gt;&gt;P: pauldot sync\n    P-&gt;&gt;R: git pull (new commits detected)\n    P-&gt;&gt;M: Rewrites ~/.zshrc automatically\n\n    Note over M: Open a new shell \u2014 ll works \u2713</code></pre>"},{"location":"flows/aliases/#step-by-step","title":"Step by step","text":""},{"location":"flows/aliases/#1-add-the-alias","title":"1. Add the alias","text":"<pre><code>pauldot alias add ll \"ls -la\"\n</code></pre> <p>This appends <code>alias ll=\"ls -la\"</code> to <code>files/aliases.zsh</code> in your dotfiles repo. If <code>git.auto_commit = true</code>, the change is committed immediately.</p>"},{"location":"flows/aliases/#2-use-it-locally","title":"2. Use it locally","text":"<p><code>alias add</code> rewrites <code>~/.zshrc</code> automatically \u2014 no separate <code>apply</code> needed. Open a new shell (or <code>source ~/.zshrc</code>) to use the alias.</p> <p>Create a reload alias</p> <p>Add a <code>reload</code> alias to your dotfiles repo that sources <code>~/.zshrc</code>, so you can run <code>reload</code> instead of opening a new shell every time you add an alias.</p>"},{"location":"flows/aliases/#3-push-to-the-remote","title":"3. Push to the remote","text":"<pre><code>pauldot sync\n</code></pre> <p><code>sync</code> runs <code>git pull --rebase</code> then pushes any local commits. After this, the alias is in the remote repo.</p>"},{"location":"flows/aliases/#4-pull-it-on-another-machine","title":"4. Pull it on another machine","text":"<p>On Machine B:</p> <pre><code>pauldot sync\n</code></pre> <p><code>sync</code> pulls the latest dotfiles and, when new commits are detected, rewrites <code>~/.zshrc</code> automatically. Open a new shell \u2014 the alias is available.</p>"},{"location":"flows/aliases/#notes","title":"Notes","text":"<ul> <li><code>pauldot alias list</code> shows all aliases currently defined in your dotfiles.</li> <li>If you want to remove an alias, edit <code>files/aliases.zsh</code> directly (<code>pauldot edit zshrc</code> opens it in <code>$EDITOR</code>), then <code>apply</code> and <code>sync</code>.</li> <li>Aliases live in <code>files/aliases.zsh</code> which is sourced by every generated <code>~/.zshrc</code>, regardless of profile.</li> </ul>"},{"location":"flows/bootstrap/","title":"Bootstrap a new machine","text":"<p>This flow covers setting up pauldot for the first time ever i.e. creating your dotfiles repo from scratch and getting your first machine under management.</p>"},{"location":"flows/bootstrap/#overview","title":"Overview","text":"<pre><code>flowchart TD\n    A([Fresh machine]) --&gt; B[Install uv]\n    B --&gt; C[\"uv tool install pauldot\"]\n    C --&gt; D{Private dotfiles repo?}\n\n    D --&gt;|Yes| E[\"pauldot help gh\"]\n    E --&gt; F[\"gh auth login\"]\n    F --&gt; G\n\n    D --&gt;|No / not yet| G[\"gh repo create you/dotfiles --private --clone\"]\n\n    G --&gt; H[\"pauldot init --scaffold ./dotfiles\"]\n    H --&gt; I[\"Edit pauldot.toml and bootstrap.sh\"]\n    I --&gt; N[\"git add . &amp;&amp; git commit -m 'init' &amp;&amp; git push\"]\n    N --&gt; O[\"pauldot init git@github.com:you/dotfiles\"]\n    O --&gt; P[\"pauldot apply\"]\n    P --&gt; Q([\"\u2713 Machine configured\"])</code></pre>"},{"location":"flows/bootstrap/#step-by-step","title":"Step by step","text":""},{"location":"flows/bootstrap/#1-install-uv","title":"1. Install uv","text":"<p>pauldot is distributed via uv. Install it first:</p> <pre><code>curl -LsSf https://astral.sh/uv/install.sh | sh\n</code></pre>"},{"location":"flows/bootstrap/#2-install-pauldot","title":"2. Install pauldot","text":"<pre><code>uv tool install pauldot\npauldot --install-completion &amp;&amp; exec zsh\n</code></pre>"},{"location":"flows/bootstrap/#3-authenticate-with-github-private-repos","title":"3. Authenticate with GitHub (private repos)","text":"<p>If your dotfiles repo will be private, set up GitHub CLI before proceeding:</p> <pre><code>pauldot help gh   # walks you through gh auth login\n</code></pre>"},{"location":"flows/bootstrap/#4-create-and-scaffold-your-dotfiles-repo","title":"4. Create and scaffold your dotfiles repo","text":"<pre><code>gh repo create you/dotfiles --private --clone\npauldot init --scaffold ./dotfiles\n</code></pre> <p>This writes the skeleton: <code>pauldot.toml</code>, <code>profiles/</code>, <code>files/</code>, <code>tools/</code>, and <code>bootstrap.sh</code>.</p>"},{"location":"flows/bootstrap/#5-configure","title":"5. Configure","text":"<p>Edit <code>pauldot.toml</code> \u2014 set your <code>default_profile</code>, <code>git.visibility</code>, and anything else you want.</p> <p>Edit <code>bootstrap.sh</code> \u2014 set <code>PAULDOT_REPO</code> to your repo's SSH URL.</p>"},{"location":"flows/bootstrap/#6-commit-and-push","title":"6. Commit and push","text":"<pre><code>cd dotfiles &amp;&amp; git add . &amp;&amp; git commit -m \"init\" &amp;&amp; git push\n</code></pre>"},{"location":"flows/bootstrap/#7-apply","title":"7. Apply","text":"<pre><code>pauldot init git@github.com:you/dotfiles\npauldot apply\n</code></pre> <p><code>apply</code> writes <code>~/.zshrc</code> from your source files and installs any declared tools.</p> <p>Open a new shell \u2014 you're done.</p>"},{"location":"flows/bootstrap/#on-every-subsequent-machine","title":"On every subsequent machine","text":"<pre><code>curl -sSL https://raw.githubusercontent.com/you/dotfiles/main/bootstrap.sh | sh\n</code></pre> <p>Or manually:</p> <pre><code>uv tool install pauldot\npauldot --install-completion &amp;&amp; exec zsh\npauldot init git@github.com:you/dotfiles\npauldot apply\n</code></pre>"},{"location":"flows/migrate/","title":"Migrate an existing machine","text":"<p>This flow covers adding pauldot to a machine that already has a <code>~/.zshrc</code> with aliases, exports, and tool config you want to keep, and an existing dotfiles repo you want to bring it under.</p> <p>The key tool here is <code>pauldot migrate</code>, which reads your existing <code>~/.zshrc</code> before pauldot touches anything and distributes the content into your dotfiles source files. You review and commit the result, then run <code>apply</code>.</p>"},{"location":"flows/migrate/#overview","title":"Overview","text":"<pre><code>flowchart TD\n    A([Machine with existing ~/.zshrc]) --&gt; B[Install uv]\n    B --&gt; C[\"uv tool install pauldot\"]\n    C --&gt; D{Private dotfiles repo?}\n\n    D --&gt;|Yes| E[\"pauldot help gh\\ngh auth login\"]\n    E --&gt; F[\"pauldot init git@github.com:you/dotfiles\"]\n    D --&gt;|No| F\n\n    F --&gt; G[\"pauldot migrate --dry-run\"]\n    G --&gt; H{Output looks right?}\n\n    H --&gt;|Something unexpected| I[Manually review ~/.zshrc\\nand edit source files in dotfiles repo]\n    I --&gt; G\n\n    H --&gt;|Yes| J[\"pauldot migrate\"]\n    J --&gt; K[\"pauldot sync\\nPushes migrated changes to remote\"]\n    K --&gt; L[\"pauldot apply\"]\n    L --&gt; M([\"\u2713 Machine configured\\n~/.zshrc is now written by pauldot\"])</code></pre>"},{"location":"flows/migrate/#what-migrate-does","title":"What <code>migrate</code> does","text":"<p><code>pauldot migrate</code> reads your current <code>~/.zshrc</code> (which must be a real file, not already a symlink) and splits it:</p> <ul> <li>Lines starting with <code>alias foo=</code> \u2192 appended to <code>files/aliases.zsh</code></li> <li>Everything else (exports, tool init, path manipulation) \u2192 written to <code>files/zshrc.base</code></li> </ul> <p>Aliases that already exist in <code>aliases.zsh</code> are skipped to avoid duplicates.</p> <p>Use <code>--dry-run</code> first to see exactly what will be written before anything changes.</p>"},{"location":"flows/migrate/#step-by-step","title":"Step by step","text":""},{"location":"flows/migrate/#1-install-uv-and-pauldot","title":"1. Install uv and pauldot","text":"<pre><code>curl -LsSf https://astral.sh/uv/install.sh | sh\nuv tool install pauldot\n</code></pre>"},{"location":"flows/migrate/#2-authenticate-and-init","title":"2. Authenticate and init","text":"<p>If your dotfiles repo is private:</p> <pre><code>pauldot help gh   # walks you through gh auth login\n</code></pre> <p>Then clone your repo and set the active profile:</p> <pre><code>pauldot init git@github.com:you/dotfiles\n</code></pre>"},{"location":"flows/migrate/#3-preview-the-migration","title":"3. Preview the migration","text":"<pre><code>pauldot migrate --dry-run\n</code></pre> <p>This shows you what would be written to <code>files/aliases.zsh</code> and <code>files/zshrc.base</code> \u2014 without touching anything.</p> <p>Check for:</p> <ul> <li>Aliases you no longer want (you can delete them from the dotfiles source files after migration)</li> <li>Tool init blocks (nvm, pyenv, rbenv, etc.) that might conflict with what's already in your base config</li> </ul>"},{"location":"flows/migrate/#4-run-the-migration","title":"4. Run the migration","text":"<pre><code>pauldot migrate\n</code></pre> <p>The result is written into your local <code>~/.pauldot/</code> repo clone. If <code>git.auto_commit = true</code> in <code>pauldot.toml</code>, the changes are committed automatically.</p>"},{"location":"flows/migrate/#5-review-and-push","title":"5. Review and push","text":"<pre><code>pauldot sync\n</code></pre> <p>This pushes the migrated changes so other machines can pull them.</p>"},{"location":"flows/migrate/#6-apply","title":"6. Apply","text":"<pre><code>pauldot apply\n</code></pre> <p>Your existing <code>~/.zshrc</code> is backed up to <code>~/.zshrc.bak.&lt;timestamp&gt;</code>. Pauldot writes a new <code>~/.zshrc</code> from your dotfiles source files. Open a new shell to pick up the changes.</p>"},{"location":"flows/migrate/#if-something-looks-wrong-after-apply","title":"If something looks wrong after apply","text":"<p>The backup is always at <code>~/.zshrc.bak.&lt;timestamp&gt;</code>. You can diff it against the generated file:</p> <pre><code>diff ~/.zshrc.bak.* ~/.zshrc\n</code></pre> <p>To adjust, edit the source files in <code>~/.pauldot/files/</code>, run <code>pauldot apply</code> again, and <code>pauldot sync</code> when you're happy.</p>"},{"location":"flows/profiles/","title":"Profile lifecycle","text":"<p>This flow covers creating a new profile, activating it, and using it across machines.</p>"},{"location":"flows/profiles/#what-profiles-are","title":"What profiles are","text":"<p>A profile is a named slice of your dotfiles config. Every machine has one active profile at a time. Profiles can extend a shared <code>base</code> profile, adding their own zshrc source file, tools, and tracked dotfiles on top.</p> <p>A typical setup:</p> <ul> <li><code>base</code> \u2014 shared across everything: aliases, common tools, starship</li> <li><code>personal</code> \u2014 extends base; adds personal PATH tweaks and personal tools</li> <li><code>work</code> \u2014 extends base; adds work PATH, work-only tools (<code>slack</code>, <code>zed</code>), a <code>.gitconfig</code> with your work email</li> </ul>"},{"location":"flows/profiles/#overview","title":"Overview","text":"<pre><code>flowchart TD\n    A([You want a work profile]) --&gt; B[\"Create profiles/work.toml\\n  extends = 'base'\\n  zshrc = 'files/zshrc.work'\\n  tools = ['slack']\\n  dotfiles = ['.gitconfig']\"]\n    B --&gt; C[\"Create files/zshrc.work\\nAdd work-specific PATH, tool init\"]\n    C --&gt; D[\"pauldot profile set work\"]\n    D --&gt; E{Apply now?}\n    E --&gt;|Yes| F[\"pauldot apply\"]\n    F --&gt; G[\"exec zsh\"]\n    G --&gt; H([\"\u2713 Work profile active\\n ~/.zshrc contains work config\\n tools reconciled\\n dotfiles copied to ~\"])\n    E --&gt;|Later| I[\"pauldot apply whenever ready\"]\n\n    H --&gt; J[\"pauldot sync\\nPushes profile files to remote\"]\n    J --&gt; K[\"On another machine:\\npauldot profile set work\\npauldot apply\"]</code></pre>"},{"location":"flows/profiles/#step-by-step","title":"Step by step","text":""},{"location":"flows/profiles/#1-create-a-profile-file","title":"1. Create a profile file","text":"<p>In your dotfiles repo, create <code>profiles/work.toml</code>:</p> <pre><code>extends = \"base\"\n\nzshrc = \"files/zshrc.work\"\n\ntools = [\"slack\", \"zed\", \"ripgrep\"]\n\ndotfiles = [\".gitconfig\"]\n</code></pre> <ul> <li><code>extends</code> \u2014 inherit everything from <code>base</code> and add on top</li> <li><code>zshrc</code> \u2014 a profile-specific source file (PATH tweaks, tool init, exports)</li> <li><code>tools</code> \u2014 tools to install on any machine running this profile</li> <li><code>dotfiles</code> \u2014 files in <code>files/home/</code> to copy to <code>~</code> on apply (if missing)</li> </ul>"},{"location":"flows/profiles/#2-create-the-profiles-zshrc-source-file","title":"2. Create the profile's zshrc source file","text":"<pre><code>touch ~/.pauldot/files/zshrc.work\n</code></pre> <p>Add any work-specific config:</p> <pre><code>export WORK_MODE=true\nexport EDITOR=\"zed --wait\"\nexport PATH=\"$HOME/.work/bin:$PATH\"\n</code></pre>"},{"location":"flows/profiles/#3-set-the-active-profile","title":"3. Set the active profile","text":"<pre><code>pauldot profile set work\n</code></pre> <p>You'll be asked whether to apply immediately. You can also pass <code>--apply</code> to skip the prompt:</p> <pre><code>pauldot profile set work --apply\n</code></pre>"},{"location":"flows/profiles/#4-apply-and-reload","title":"4. Apply and reload","text":"<pre><code>pauldot apply\nexec zsh\n</code></pre> <p><code>apply</code> writes <code>~/.zshrc</code> from the work profile's source files, reconciles tool installs, and links any tracked dotfiles listed in <code>dotfiles</code>.</p>"},{"location":"flows/profiles/#switching-back","title":"Switching back","text":"<pre><code>pauldot profile set personal --apply\nexec zsh\n</code></pre> <p>The previous profile's zshrc content is replaced; tools from the new profile are reconciled on the next apply.</p>"},{"location":"flows/profiles/#using-a-profile-on-another-machine","title":"Using a profile on another machine","text":"<pre><code>pauldot profile set work\npauldot apply\n</code></pre> <p>The profile file (<code>profiles/work.toml</code>) is already in the repo. Setting it and applying is all that's needed.</p>"},{"location":"flows/profiles/#notes","title":"Notes","text":"<ul> <li><code>pauldot profile list</code> shows all available profiles.</li> <li><code>pauldot profile show</code> shows the currently active profile.</li> <li>Only one profile is active per machine at a time. Each machine can run a different profile from the same repo.</li> <li><code>extends</code> is single-level only \u2014 <code>work</code> extends <code>base</code>, but <code>base</code> cannot itself extend another profile.</li> </ul>"},{"location":"flows/tools/","title":"Tool lifecycle","text":"<p>This flow covers declaring a new tool, installing it on the current machine, syncing the declaration to the remote, and having it automatically installed on another machine.</p>"},{"location":"flows/tools/#overview","title":"Overview","text":"<pre><code>sequenceDiagram\n    actor U as You (Machine A)\n    participant P as pauldot\n    participant T as Tool (e.g. ripgrep)\n    participant R as Dotfiles repo (GitHub)\n    actor M as Machine B\n\n    U-&gt;&gt;P: pauldot tool add\n    P-&gt;&gt;U: Prompt: name?\n    U-&gt;&gt;P: ripgrep\n    P-&gt;&gt;U: Prompt: check command?\n    U-&gt;&gt;P: command -v rg\n    P-&gt;&gt;U: Prompt: install (macOS)?\n    U-&gt;&gt;P: brew install ripgrep\n    P-&gt;&gt;U: Prompt: install (Linux)?\n    U-&gt;&gt;P: sudo apt install ripgrep\n    P-&gt;&gt;R: Appends entry to tools/tools.toml\n    P-&gt;&gt;R: git commit (if auto_commit = true)\n\n    U-&gt;&gt;P: pauldot apply\n    P-&gt;&gt;T: Runs check: command -v rg\n    T-&gt;&gt;P: Not found\n    P-&gt;&gt;T: Runs install command for current OS\n    T-&gt;&gt;P: \u2713 Installed\n    P-&gt;&gt;U: Summary table\n\n    Note over U: rg is now available \u2713\n\n    U-&gt;&gt;P: pauldot sync\n    P-&gt;&gt;R: git push\n\n    Note over M: Later, on Machine B\n\n    M-&gt;&gt;P: pauldot sync\n    P-&gt;&gt;R: git pull\n    P-&gt;&gt;M: tools/tools.toml updated locally\n\n    M-&gt;&gt;P: pauldot apply\n    P-&gt;&gt;T: Runs check: command -v rg\n    T-&gt;&gt;P: Not found\n    P-&gt;&gt;T: Runs install command for Machine B's OS\n    T-&gt;&gt;P: \u2713 Installed\n    P-&gt;&gt;M: Summary table\n\n    Note over M: rg is now available \u2713</code></pre>"},{"location":"flows/tools/#step-by-step","title":"Step by step","text":""},{"location":"flows/tools/#1-declare-the-tool","title":"1. Declare the tool","text":"<pre><code>pauldot tool add\n</code></pre> <p>You'll be prompted for:</p> <ul> <li>Name \u2014 a short identifier (e.g. <code>ripgrep</code>)</li> <li>Check command \u2014 a shell expression that exits 0 if the tool is present (e.g. <code>command -v rg</code>)</li> <li>Install command (macOS) \u2014 what to run to install it on macOS</li> <li>Install command (Linux) \u2014 what to run to install it on Linux</li> </ul> <p>Either install command can be left blank \u2014 the tool is silently skipped on that OS.</p> <p>The entry is appended to <code>tools/tools.toml</code>:</p> <pre><code>[[tool]]\nname = \"ripgrep\"\ncheck = \"command -v rg\"\ninstall.macos = \"brew install ripgrep\"\ninstall.linux = \"sudo apt install ripgrep\"\n</code></pre>"},{"location":"flows/tools/#2-install-it-locally","title":"2. Install it locally","text":"<pre><code>pauldot apply\n</code></pre> <p>For each tool in your active profile, <code>apply</code> runs the check command. If it exits non-zero (tool missing), the install command for the current OS is executed and its output streams live to the terminal. The result is printed in a summary table.</p> <p>Tool install failures do not abort the loop \u2014 each failure is reported and the run continues.</p> <p>To install a single tool by name without a full apply:</p> <pre><code>pauldot tool install ripgrep\n</code></pre>"},{"location":"flows/tools/#3-push-to-the-remote","title":"3. Push to the remote","text":"<pre><code>pauldot sync\n</code></pre> <p>Pushes the updated <code>tools/tools.toml</code> to the remote.</p>"},{"location":"flows/tools/#4-install-on-another-machine","title":"4. Install on another machine","text":"<p>On Machine B:</p> <pre><code>pauldot sync    # pulls the latest dotfiles, including the new tool declaration\npauldot apply   # detects the tool is missing, runs the install command\n</code></pre> <p>The right install command is picked automatically based on that machine's OS.</p>"},{"location":"flows/tools/#notes","title":"Notes","text":"<ul> <li><code>pauldot tool list</code> shows all declared tools and whether each is currently installed.</li> <li>Tools are declared globally in <code>tools/tools.toml</code> but only installed if they appear in the active profile's <code>tools = [...]</code> list.</li> <li>To remove a tool declaration: <code>pauldot tool remove &lt;name&gt;</code>. This removes it from <code>tools.toml</code> but does not uninstall the binary from your system.</li> <li>OS detection is centralised \u2014 <code>macos</code> or <code>linux</code> only. The matching install command is chosen at apply time.</li> </ul>"},{"location":"flows/track/","title":"Track a dotfile","text":"<p>This flow covers tracking a dotfile (like <code>.gitconfig</code>) in your repo and keeping it in sync across machines.</p>"},{"location":"flows/track/#what-tracking-means","title":"What tracking means","text":"<p>\"Tracking\" a dotfile means copying it into your dotfiles repo under <code>files/home/</code> and adding it to the active profile's <code>dotfiles</code> list. On every subsequent <code>apply</code>, pauldot copies the repo version to <code>~</code> if the file is missing on that machine.</p> <p>The live file stays a real file \u2014 not a symlink. You edit it normally. When you run <code>pauldot sync</code>, the live version is copied back into the repo and pushed.</p>"},{"location":"flows/track/#overview","title":"Overview","text":"<pre><code>sequenceDiagram\n    actor U as You (Machine A)\n    participant P as pauldot\n    participant R as Dotfiles repo (GitHub)\n    actor M as Machine B\n\n    U-&gt;&gt;P: pauldot track ~/.gitconfig\n    P-&gt;&gt;R: Copies ~/.gitconfig to files/home/.gitconfig\n    P-&gt;&gt;R: Adds \".gitconfig\" to active profile's dotfiles list\n    P-&gt;&gt;R: git commit (if auto_commit = true)\n    Note over U: ~/.gitconfig is unchanged \u2014 still a real file \u2713\n\n    Note over U: Edit ~/.gitconfig normally (apps write to it freely)\n\n    U-&gt;&gt;P: pauldot sync\n    P-&gt;&gt;R: Copies ~/.gitconfig \u2192 files/home/.gitconfig (live is source of truth)\n    P-&gt;&gt;R: git push\n\n    Note over U: pauldot status shows drift if live differs from repo\n\n    Note over M: Later, on Machine B\n\n    M-&gt;&gt;P: pauldot sync\n    P-&gt;&gt;R: git pull (new commits detected)\n    P-&gt;&gt;M: pauldot apply (auto-triggered)\n    P-&gt;&gt;M: Copies files/home/.gitconfig \u2192 ~/.gitconfig (only if missing)\n\n    Note over M: ~/.gitconfig matches Machine A \u2713</code></pre>"},{"location":"flows/track/#step-by-step","title":"Step by step","text":""},{"location":"flows/track/#1-track-the-file","title":"1. Track the file","text":"<pre><code>pauldot track ~/.gitconfig\n</code></pre> <p>This:</p> <ol> <li>Copies <code>~/.gitconfig</code> to <code>files/home/.gitconfig</code> in your repo</li> <li>Adds <code>\".gitconfig\"</code> to the active profile's <code>dotfiles</code> list in <code>profiles/&lt;name&gt;.toml</code></li> <li>Commits the change if <code>git.auto_commit = true</code></li> </ol> <p>Your local <code>~/.gitconfig</code> is untouched.</p>"},{"location":"flows/track/#2-edit-the-file-normally","title":"2. Edit the file normally","text":"<p>You don't need to do anything special. Apps like <code>git config --global</code> write directly to <code>~/.gitconfig</code>. That's fine \u2014 the live file is always the source of truth.</p>"},{"location":"flows/track/#3-sync-live-changes-to-the-repo","title":"3. Sync live changes to the repo","text":"<pre><code>pauldot sync\n</code></pre> <p><code>sync</code> copies <code>~/.gitconfig</code> \u2192 <code>files/home/.gitconfig</code>, commits, and pushes. Any drift between the live file and the repo version is captured here.</p> <p>To preview what's out of sync before pushing:</p> <pre><code>pauldot status\n</code></pre>"},{"location":"flows/track/#4-bootstrap-on-another-machine","title":"4. Bootstrap on another machine","text":"<p>On a fresh machine after <code>pauldot init</code> and <code>pauldot apply</code>:</p> <pre><code>pauldot apply\n</code></pre> <p>For each file in the active profile's <code>dotfiles</code> list that doesn't exist on the machine, pauldot copies it from <code>files/home/</code> to <code>~/</code>. Existing files are left alone unless you pass <code>--overwrite</code>:</p> <pre><code>pauldot apply --overwrite   # copies repo version to ~/, backs up the existing file first\n</code></pre>"},{"location":"flows/track/#file-layout","title":"File layout","text":"<p>Tracked dotfiles live under <code>files/home/</code> in the repo, mirroring their location relative to <code>~</code>:</p> <pre><code>files/home/\n\u251c\u2500\u2500 .gitconfig\n\u2514\u2500\u2500 .config/\n    \u2514\u2500\u2500 starship.toml\n</code></pre> <p><code>files/home/.gitconfig</code> \u2192 <code>~/.gitconfig</code> <code>files/home/.config/starship.toml</code> \u2192 <code>~/.config/starship.toml</code></p>"},{"location":"flows/track/#notes","title":"Notes","text":"<ul> <li>Tracking is per-profile. A file tracked in <code>work</code> won't appear on a machine running <code>personal</code> unless you also add it to <code>personal</code>.</li> <li><code>pauldot apply</code> only copies files that are missing on the target machine. It never silently overwrites a file that already exists locally. Use <code>--overwrite</code> (with backup) to pull the repo version.</li> <li>If both machines edit a tracked file between syncs, <code>pauldot sync</code> will detect the conflict and eject \u2014 resolve it manually or run <code>pauldot apply --overwrite</code> to accept the repo version.</li> <li>For files that tools write to constantly (shell history, browser state), don't track them \u2014 they'll conflict on every sync.</li> </ul>"}]}